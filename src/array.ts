function PrintArr(arr:number[]) {
    for (let i = 0; i < arr.length; i++) {
        console.log(arr[i])
    }
}

interface Manager {
    firstName:string,
    lastName:string,
    department:string
}

function ArrOfManagers(manager:Manager[]) {
    //.....
}